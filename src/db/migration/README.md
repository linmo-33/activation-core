# 导出导入基础设施

该目录用于后续已部署系统迁移到 Drizzle 新架构时的导出/导入实现。

当前阶段只建立基础骨架，不在此时直接接入生产迁移逻辑。

规划内容：

- 导出数据结构定义
- 导出脚本
- 导入脚本
- 数据校验与清洗

当前关键表范围：

- `admin_users`
- `activation_codes`

说明：

- `admin_login_guards` 默认不迁移
- 运行时保护状态在新系统中重新累积
